---
# cPanel Git Version Control deploy (nincs SSH/Terminal).
# A "Pull or Deploy → Deploy HEAD" gomb futtatja. Egyetlen taszk: a script a SAJÁT
# helyéből vezeti le a repó útját, így nem kell env-öröklődés a taszkok közt.
deployment:
  tasks:
    - /bin/bash /home/elitediv/visualbyadam.hu/zsolti_crm/deploy/cpanel-deploy.sh
